  <!-- <div id="profile">
    <h2 class="ps">
      <% if !current_user.avatar_url.nil?%>
        <img src="<%= current_user.avatar_url %>" height="80" width="80">Hi, <%= @user.username %>
      <% else %>
        <%= gravatar_for_eighty_px @user %> Hi, <%= @user.username %>
      <% end %>
    </h2>
    <p id='add-photo'><%= link_to 'Add', 'https://en.gravatar.com/', class: 'edit-link' %> profile picture.</p>
    <%= render 'follow_form' if logged_in? %>

    <section class="stats">
      <%= render 'shared/stats' %>
    </section><br>

    <div id="profile-text">
      <p>Email: <%= @user.email %></p>
      <p>Bio: <%= @user.bio %></p>
      <%= link_to "Edit Profile", edit_user_path(current_user), class: "edit-link" %>
    </div>
      <div id="newsfeed">
        <h3 id="postnewstext">Post NewsFeed</h3>
        <% if @feed_items.any? %>
        <% @feed_items.each do |post| %>
        <ul>
          <li>Title: <%= link_to post.title, post_path(post), class: "edit-link" %></li>
          <li>Author: <%= link_to post.user.username, user_path(post.user.id), class: "edit-link" %></li>
          <li>Created: <%= link_to post.created_at.strftime("%m/%d/%y @ %I:%M%p"), static_path(post.id), class: "edit-link"  %></li>
        </ul>
        <% end %>
        <% end %>
      </div>
  </div>


  </div>
    <div id="my-communities">
    <h2>My Communities</h2>
    <p><%= link_to "Browse Communities", communities_path, class: 'edit-link'%></p>
    <% @user.communities.each do |community| %>
    <ul>
      <div>
        <li> <%=link_to community.name, community_path(community), class: 'edit-link'%><%= link_to " | unsubscribe", unsubscribe_path(community), method: :delete, class: 'unsub-link-to-text-color' %>
        </li>
      </div>
    </ul>
    <% end %>
  </div> -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
  <link rel="stylesheet" href="https://bootswatch.com/cosmo/bootstrap.min.css">
  <div class="mainbody container-fluid">
      <div class="row">
          <div class="container-fluid">
          <div style="padding-top:50px;"> </div>
          <div class="col-lg-3 col-md-3 hidden-sm hidden-xs">
              <div class="panel panel-default">
                  <div class="panel-body">
                      <div class="media">
                          <div align="center">
                              <img class="thumbnail img-responsive" src="<%= @user.avatar_url %>" width="200px" height="200px">
                          </div>
                          <div class="media-body">
                            <h3><strong><%= @user.username %></strong></h3>
                            <p><%= render 'shared/stats' %></p>
                            <%= render 'follow_form' if logged_in? %>

                              <hr>
                              <h3><strong>Bio</strong></h3>
                              <p><%= @user.bio %></p>
                              <hr>
                              <h3><strong>Email</strong></h3>
                              <p> <%= @user.email %></p>
                              <hr>
                              <h3><strong><%= link_to "Edit Profile", edit_user_path(current_user), class: "edit-link" %></strong></h3>

                          </div>
                      </div>
                  </div>
              </div>
          </div>
          <div class="col-lg-9 col-md-9 col-sm-12 col-xs-12">
              <div class="panel panel-default">
                  <div class="panel-body">
                    <h3 id="postnewstext">Post NewsFeed</h3>
                    <% if @feed_items.any? %>
                    <% @feed_items.each do |post| %>
                    <ul>
                      <li>Title: <%= link_to post.title, post_path(post), class: "edit-link" %></li>
                      <li>Author: <%= link_to post.user.username, user_path(post.user.id), class: "edit-link" %></li>
                      <li>Created: <%= link_to post.created_at.strftime("%m/%d/%y @ %I:%M%p"), static_path(post.id), class: "edit-link"  %></li>
                    </ul>
                    <% end %>
                    <% end %>

                      <br><br>

                  </div>
              </div>
              <hr>
              <!-- Simple post content example. -->
              <div class="panel panel-default">
                  <div class="panel-body">
                    <h2>My Communities</h2>
                    <p><%= link_to "Browse Communities", communities_path, class: 'edit-link'%></p>
                    <% @user.communities.each do |community| %>
                    <ul>
                      <div>
                        <li> <%=link_to community.name, community_path(community), class: 'edit-link'%><%= link_to " | unsubscribe", unsubscribe_path(community), method: :delete, class: 'unsub-link-to-text-color' %>
                        </li>
                      </div>
                    </ul>
                    <% end %>
                      
                      </div>
                  </div>
              </div>
          </div>
      </div>
  </div>
