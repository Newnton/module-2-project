<div class="">
  <h1><%= @post.title %></h1>
  <%= @post.content %><br>
  <br>
</div>

<div class="">
  <h2>Comments:</h2>
  <%= link_to "add a comment", new_comment_path %>
  <ul>
    <% @post.comments.each do |comment| %>
    <li><%= comment.content %> <br> <%= comment.user.username %></li>
    <% end %>
  </ul>
</div>
